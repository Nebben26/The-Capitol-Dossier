Before doing any work, run these checks:

1. Read CLAUDE.md, rules.md, docs/API_REFERENCE.md, and docs/FRONTEND_CONTRACT.md
2. Run `npm run build` to establish current state — note any existing errors
3. Check `lib/api.ts` to see which functions are returning real data vs mock data
4. Check `lib/mockData.ts` to understand current type definitions
5. Check `hooks/useData.ts` to understand how components consume data
6. Report status:
   - Which APIs are currently integrated (real data)?
   - Which APIs are still returning mock data?
   - Any build errors?
   - What's the next day's task?

Then wait for instructions on which day to execute.
